import React from 'react';

interface MapProps {
  source: string;
  destination: string;
  percentComplete: number;
}

export default function Map({ source, destination, percentComplete }: MapProps) {
  return <></>;
}
